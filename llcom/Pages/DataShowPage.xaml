<Page
    x:Class="llcom.Pages.DataShowPage"
    x:Name="DataShowPageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
    xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fa="http://schemas.fontawesome.io/icons/"
    xmlns:local="clr-namespace:llcom.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="DataShowPage"
    adonisExtensions:RippleExtension.FadeInDuration="0"
    adonisExtensions:RippleExtension.FadeOutDuration="0"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Loaded="Page_Loaded"
    mc:Ignorable="d">
    <Page.Resources>
        <local:boolVisibe x:Key="boolVisibeConverter" />
        <local:boolNotVisibe x:Key="boolNotVisibeConverter" />
        <local:showHexFormat x:Key="showHexFormatConverter" />
        <Style x:Key="DataShowStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <RichTextBox
                            Name="ItemGrid"
                            Background="Transparent"
                            BorderThickness="0"
                            FontFamily="Consolas,Microsoft YaHei,微软雅黑"
                            FontSize="12"
                            Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"
                            IsReadOnly="True">
                            <FlowDocument>
                                <Paragraph Margin="0">
                                    <Run Text="{Binding TimeText}" />
                                    <Run Text="{Binding ArrowText}" />
                                    <Run
                                        FontSize="15"
                                        Foreground="{Binding DataTextColor}"
                                        Text="{Binding DataText}" />
                                    <Run
                                        FontWeight="Bold"
                                        Text="{Binding RawTitle}" />
                                    <Run
                                        FontSize="15"
                                        Foreground="{Binding RawTextColor}"
                                        Text="{Binding RawText}" />
                                    <Run Foreground="{Binding HexTextColor}" Text="{Binding HexText}" />
                                </Paragraph>
                            </FlowDocument>
                        </RichTextBox>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="ItemGrid" Property="Background" Value="Transparent" />
                            </Trigger>
                            <Trigger Property="Selector.IsSelected" Value="true">
                                <Setter TargetName="ItemGrid" Property="Background" Value="Transparent" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <Grid x:Name="mainGrid" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBox
            Name="MainTextBox"
            VerticalContentAlignment="Top"
            Background="Transparent"
            BorderThickness="0"
            FontFamily="Consolas,Microsoft YaHei,微软雅黑"
            FontSize="15"
            Foreground="Lime"
            IsReadOnly="True"
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            TextWrapping="Wrap" />
        <ScrollViewer Name="MainListScrollViewer" VerticalScrollBarVisibility="Auto">
            <ItemsControl
                Name="MainList"
                Background="Transparent"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                VirtualizingStackPanel.CacheLength="2,2"
                VirtualizingStackPanel.IsVirtualizing="True"
                VirtualizingStackPanel.VirtualizationMode="Recycling">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <RichTextBox
                            Background="Transparent"
                            BorderThickness="0"
                            FontFamily="Consolas,Microsoft YaHei,微软雅黑"
                            FontSize="12"
                            Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"
                            IsReadOnly="True">
                            <FlowDocument>
                                <Paragraph Margin="0">
                                    <Run Text="{Binding TimeText}" />
                                    <Run Text="{Binding ArrowText}" />
                                    <Run
                                        FontSize="15"
                                        Foreground="{Binding DataTextColor}"
                                        Text="{Binding DataText}" />
                                    <Run
                                        FontWeight="Bold"
                                        Text="{Binding RawTitle}" />
                                    <Run
                                        FontSize="15"
                                        Foreground="{Binding RawTextColor}"
                                        Text="{Binding RawText}" />
                                    <Run Foreground="{Binding HexTextColor}" Text="{Binding HexText}" />
                                </Paragraph>
                            </FlowDocument>
                        </RichTextBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <Grid Grid.Row="1" Margin="10,0,10,3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Bottom">
                <CheckBox
                    Name="HEXBox"
                    Margin="5,0,0,0"
                    Content="{DynamicResource ShowHexFormat}"
                    IsChecked="{Binding showHexFormat, Converter={StaticResource showHexFormatConverter}}"
                    IsThreeState="True"
                    ToolTip="{DynamicResource ShowHexFormatTip}" />
                <CheckBox
                    Name="EnableSymbolCheckBox"
                    Margin="5,0,0,0"
                    Content="{DynamicResource ShowSymbol}"
                    IsChecked="{Binding EnableSymbol}"
                    ToolTip="{DynamicResource ShowSymbolTooltip}" />
                <CheckBox
                    Name="DisableLogCheckBox"
                    Margin="5,0,0,0"
                    Content="{DynamicResource DisableLog}"
                    IsChecked="{Binding DisableLog}" />
                <CheckBox
                    Name="LockLogCheckBox"
                    Margin="5,0,0,0"
                    Content="{DynamicResource LockLogButton}"
                    IsChecked="{Binding LockLog, ElementName=DataShowPageRoot, Mode=TwoWay}"
                    ToolTip="{DynamicResource LockLogButton}" />
                <ComboBox
                    Name="recvScriptComboBox"
                    MinWidth="120"
                    Margin="5,0,0,0"
                    VerticalAlignment="Center"
                    ToolTip="{DynamicResource RecvScriptTab}"
                    DropDownOpened="RecvScriptComboBox_DropDownOpened"
                    SelectionChanged="RecvScriptComboBox_SelectionChanged"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button
                    Name="ClearLogButton"
                    Margin="5,0,0,0"
                    Padding="8,2"
                    Click="ClearLogButton_Click"
                    Content="{DynamicResource ClearLog}" />
                <Button
                    Name="SaveLogButton"
                    Margin="5,0,0,0"
                    Padding="8,2"
                    Click="SaveLogButton_Click"
                    Content="{DynamicResource SaveLogButtonTip}"
                    ToolTip="{DynamicResource SaveLogButtonTip}" />
                <Button
                    Name="OpenLogFolderButton"
                    Margin="5,0,0,0"
                    Padding="8,2"
                    Click="OpenLogFolderButton_Click"
                    Content="{DynamicResource SettingOpenLogFolder}" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
