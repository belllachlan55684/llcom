---
description: llcom 项目修改后自动编译，若 exe 运行中则先强制结束
alwaysApply: true
---

# llcom 修改后编译规则

在本项目（llcom）中，**每次修改完代码后**必须执行编译；若 llcom.exe 正在运行，需先强制结束再编译。

## 1. 结束运行中的 exe

编译前先检查并强制结束 llcom.exe：

```powershell
taskkill /F /IM llcom.exe 2>$null
```

- `/F`：强制终止
- `2>$null`：忽略“进程不存在”等错误，避免脚本报错

## 2. 执行编译

在**工作区根目录**（含 `llcom.sln` 的目录）执行：

```powershell
msbuild llcom.sln /p:Configuration=Debug /p:Platform=x64 /t:Build
```

- 默认使用 Debug|x64；若需 Release 或 x86，可改为 `/p:Configuration=Release` 或 `/p:Platform=x86`
- 需已安装 Visual Studio 或 Build Tools，确保 `msbuild` 在 PATH 中

## 3. 完整流程示例

```powershell
# 1. 结束可能运行的 llcom.exe
taskkill /F /IM llcom.exe 2>$null

# 2. 编译
msbuild llcom.sln /p:Configuration=Debug /p:Platform=x64 /t:Build
```

## 4. 输出路径

- Debug x64：`llcom\bin\x64\Debug\llcom.exe`
- Debug x86：`llcom\bin\x86\Debug\llcom.exe`
- Release x64：`llcom\bin\x64\Release\llcom.exe`
- Release x86：`llcom\bin\x86\Release\llcom.exe`
